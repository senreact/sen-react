{
  "ignoreCommand": "[ \"$VERCEL_ENV\" = \"production\" ] && exit 1; if ! git cat-file -e \"$VERCEL_GIT_PREVIOUS_SHA\" 2>/dev/null; then exit 1; fi; git diff --quiet \"$VERCEL_GIT_PREVIOUS_SHA\" HEAD -- apps/web packages/shared pnpm-lock.yaml package.json tsconfig.base.json tsconfig.json eslint.config.mjs"
}
