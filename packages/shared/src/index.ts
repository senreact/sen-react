/**
 * @sen-react/shared — types, schemas, utilities shared across web + cms.
 *
 * This package is the single source of truth for cross-app contracts. Avoid
 * runtime dependencies that don't run in both Node and the browser.
 */

export * from "./locale";
export * from "./sectors";
